import SourceDownloader from "./source_downloader";

/**
 * Handles downloading and caching audio
 */
class AudioDownloader {
  download_queue_: Array<SourceDownloader>;

  constructor() {
    //
  }
}